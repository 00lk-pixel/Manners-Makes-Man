# 가입자 정보(이름/이메일)가 담기는 로컬 목업 저장소 - 절대 커밋하지 않음
data/signups.json

# Streamlit 로컬 secrets (Google OAuth 등) - 절대 커밋하지 않음
.streamlit/secrets.toml

# build_standalone.py 산출물(MMM_standalone.html, 약 80MB)은 제출용이라 예외적으로 커밋한다.
# 화면 파일을 수정하면 python build_standalone.py로 재생성해서 같이 커밋할 것.
